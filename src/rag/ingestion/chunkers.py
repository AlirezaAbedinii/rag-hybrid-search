"""Chunkers: fixed | recursive | semantic, switchable via config.

MVP: fixed-size-with-overlap. V1: structure-aware recursive + semantic.
Each chunk is tagged with its strategy and a stable chunk_id."""
